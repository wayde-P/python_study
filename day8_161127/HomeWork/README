本程序简单的实现了文件的上传和下载.
暂时只支持上传到服务器运行的文件夹
下载只支持下载到client客户端所在的文件夹.
对于已经存在的文件会在后面加上 _new 的后缀
上传文件的命令为 put filename
下载的命令为  get filename
例如:
    get a.txt
    put a.txt

执行命令直接执行即可.但是不支持交互型的命令
例如:
    df
    ls
    top -bn1